# 認証方法は以下の2通り（どちらか一方で動作します）
#
# [推奨] claude CLI のサブスクリプション認証を使う場合:
#   $ claude login   # ← ブラウザでログインするだけ。キー不要。
#   アプリが起動時に ~/.claude/.credentials.json を自動で読み込みます。
#
# [代替] Anthropic API キーを使う場合:
#   console.anthropic.com で発行し、以下に設定してください。
ANTHROPIC_API_KEY=
